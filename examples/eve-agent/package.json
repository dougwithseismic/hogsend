{
  "name": "eve-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal Eve agent service — retention-strategist with HITL. Calls back to Hogsend via POST /v1/webhooks/eve.",
  "scripts": {
    "dev": "node --env-file=.env --watch src/index.ts",
    "start": "node --env-file=.env src/index.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.22",
    "@hono/node-server": "^2.0.4"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=24"
  }
}
