---
"@hogsend/cli": patch
---

Refresh the bundled agent skills (`hogsend-authoring-journeys`) to teach `ctx.waitForEvent`, and to fill in the previously-undocumented `ctx.sleepUntil`/`ctx.when` primitives and the `"exited"` journey state.
