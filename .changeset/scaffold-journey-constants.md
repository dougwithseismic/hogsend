---
"create-hogsend": minor
---

Scaffold wires `journeyConstants: { templates: Templates, events: Events }` into `createHogsendClient` (both entry points) so newly-created apps get exact Studio journey-email previews and stable, join-safe wait/digest node metrics out of the box.
