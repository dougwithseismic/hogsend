---
"@hogsend/studio": patch
---

Journey detail: the Definition and Funnel cards move off the top of the page and into the flow's side panel, shown when no node is selected. The workflow is now the first thing on the page; selecting a node still swaps the panel to the node inspector. The funnel restacks vertically (label, drop badge, share of enrolled, count, ratio bar per stage) so it reads cleanly at panel width, and the node-type legend is gone — the "Left the journey" strip closes the funnel instead.
