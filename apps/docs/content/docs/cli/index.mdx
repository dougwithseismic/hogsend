---
title: CLI Reference
description: Install and use @hogsend/cli — the agent-native companion for inspecting and operating a running Hogsend instance from your terminal.
---

## Overview

`@hogsend/cli` is the **agent-native Hogsend CLI**. It talks to a running instance's admin API to inspect journeys, contacts, events, and metrics, probe health, and handle a few local-project chores (onboarding, skills, eject, patch). Every command accepts `--json` so agents and scripts get structured output.

It is **not** a project scaffolder and not a deployment tool — you create an app with [`create-hogsend`](/docs/getting-started/installation) and deploy it however you like.

## Installation

Install it from npm, globally or per-project:

```bash
pnpm add -g @hogsend/cli      # global `hogsend` bin
# or run without installing:
pnpm dlx @hogsend/cli doctor
```

Verify:

```bash
hogsend --version
```

## Connecting to an instance

The data commands (`doctor`, `journeys`, `contacts`, `stats`, `events`) call a running instance's API. Point the CLI at it with global flags or environment variables:

| Global flag | Env var | Default |
|---|---|---|
| `--url <baseUrl>` | `HOGSEND_API_URL` | `http://localhost:3002` |
| `--admin-key <key>` | `HOGSEND_ADMIN_KEY` / `ADMIN_API_KEY` | — |
| `--json` | — | off (human output) |

`doctor` reads the unauthenticated health route, so it needs no admin key. The other data commands wrap `/v1/admin/*` and require one.

## Commands

| Command | Description |
|---|---|
| [`hogsend doctor`](/docs/cli/doctor) | Probe a running instance's health (`GET /v1/health`) |
| [`hogsend journeys`](/docs/cli/journeys) | List, inspect, enable, and disable journeys |
| [`hogsend contacts`](/docs/cli/contacts) | List, inspect, and trace contact activity |
| [`hogsend stats`](/docs/cli/stats) | System-wide overview metrics |
| [`hogsend events`](/docs/cli/events) | Stream a single user's event history |
| [`hogsend setup`](/docs/cli/setup) | Local onboarding for a scaffolded app |
| [`hogsend skills`](/docs/cli/skills) | Install bundled Claude Code skills into `.claude/skills` |
| [`hogsend eject`](/docs/cli/eject) | Vendor a `@hogsend/*` package into `vendor/<name>` |
| [`hogsend patch`](/docs/cli/patch) | Patch a package via pnpm's native patch flow |

Run `hogsend <command> --help` for command-specific options, or `hogsend` with no arguments for the root help.

## Output for agents

Pass `--json` to any command to get a single machine-readable JSON document on stdout and nothing else. Exit codes are meaningful — `doctor`, for example, exits non-zero when an instance is unreachable, degraded, or has pending migrations — so you can branch on them in scripts.
