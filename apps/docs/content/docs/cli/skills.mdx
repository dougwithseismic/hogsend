---
title: hogsend skills
description: Manage the Claude Code skills bundled with @hogsend/cli — list them and copy them into your project's .claude/skills directory.
---

Manage the Claude Code skills bundled with `@hogsend/cli`. Bundled skills teach agents how to drive the hogsend CLI; `add` copies them into your project's `./.claude/skills/<name>/` so Claude Code can discover them.

The bundle includes two flagship flows for working **outside** a Hogsend app: `hogsend-integrate` (wire an existing product codebase to a running instance via `@hogsend/client`) and `hogsend-migrate` (migrate off Loops, Customer.io, or Resend Broadcasts with a dual-write cutover). See [Hogsend for AI agents](/docs/agents) for the full catalog, and [`/llms.txt`](https://docs.hogsend.com/llms.txt) for the stable machine entrypoint.

```bash
hogsend skills <subcommand> [options]
```

## Subcommands

| Subcommand | Description |
|---|---|
| `list` | List bundled skills and whether each is installed. |
| `add [name] [--force]` | Copy a bundled skill into `./.claude/skills/<name>/`. Omit `name` for an interactive multiselect (human), or copy all bundled skills (`--json` / non-interactive). |

## Options

| Option | Description |
|---|---|
| `--force` | Overwrite an already-installed skill. |
| `--json` | Emit machine-readable JSON only (implies non-interactive). |
| `-h, --help` | Show this help. |

## Examples

```bash
hogsend skills list
hogsend skills list --json
hogsend skills add
hogsend skills add hogsend-cli --force
```
