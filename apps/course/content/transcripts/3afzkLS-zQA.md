hey today we're going to talk about post toog identify and Alias postt identify enables you to link events from the same user across uh different sessions devices platforms uh it's very helpful to get a full picture of how a specific user interacting with your app uh over time and over different devices uh it's typically it's typically something you only use on the front end um um the reason this is is that when post hog initializes uh on the front end what we do is we create an anonymous ID for the user and we use this Anonymous ID to capture events and we continue using this Anonymous ID until uh you're able to identify who the user is so perhaps you know once they've logged in or sign in and you have their email or user ID on the other hand when you're capturing events in the back end you always have to provide a distinct ID for the user uh and so by definition you already know who they are and there's no concept of an an anonymous ID so this is why identify something you you call in the uh client side apps right uh let's jump into our sample project so uh I've created here a brief sample project where user can sign in um they can enter in their email they can enter in a password um and then it takes you to a dashboard with just a random number generator uh and this random G random number generator calls an API um right postt has already been set up for this app so if we go to the uh activity tab in post hog and we see the events we see that all the events captured here are captured with an anonymous ID and this is the anonymous ID that's automatically generated by postl let's go ahead now and um and use post talk identify in our code so the best place to call identify is as soon as you know who the user is and this is typically after they've logged in or signed up so over here I have a sign in button uh and let's assume that you know once the user CL uh clicks the button and they've already been authenticated um and they've been authenticated successfully uh we know who they are so here we want to call Post hog do identify now you have to provide an argument um for the identify call and this is this is this should be the user's distinct ID typically what we recommend you use here is either the user's email or their um or the unique ID on your database the important thing here is that this ID should be unique to that user if it's not unique you'll end up accidentally combining the same uh different users into the same profile and post so that won't be helpful okay so now that we've added post talk identify let's save our changes uh and let's go and sign in again okay so we go to our app let's enter in name Max the Hedgehog email maxos hog.com and just a random password we click sign in uh and now our identify C should have been um should have been called let's just generate some events and now when we go to the Post talk activity tab we'll see that events are starting to be captured instead of the anonymous ID we have now the users's email so all the events are are captured using uh are identified now and this also combines any previous events so for example the ones here captured with Anonymous ID it combines them into this into the same user profile well now let's Implement a post reset um and this is something that you want to implement when a user signs out so when a user signs out it probably means that a different user is going to sign in and you don't want to associate any previous uh events with a new user um a new different user let's say um and so to do this you go to your sign out uh let's go to our sign out button and here we will call Post hog.

reset with a a a par an argument true to reset the device ID uh and what this is going to do is just generate a new Anonymous ID uh and not use the old one uh and this makes sure that um uh a future user is going to have its own unique ID so once we've implemented that we save our changes we can go back to our app uh we could log in again let same next the Hedgehog ma post.com sign in um and now what happens when we click sign out is it's going to call postt reset let's just refresh the few the page a few times generate new events and when we go to our post talk activity tab we should see that we have a new Anonymous ID for user there we go so you can see here we have a different new Anonymous ID the old events are associated with with the person we identified but now future events are associated with a different Anonymous ID the last thing we're going to talk about is Alias Alias is a feature by uh by poog which enables you to assign U multiple distinct IDs to the same user so for example you could use a user's email as their distinct ID but you also may want to use their um their database ID U that that's the their ID that's in your database uh as a distinct ID and the reason you may want to do this is let's say on the front end you have easy access to their email uh and so you prefer to capture event using their uh email as as their distinct ID but on the back end there may be some parts of your code where you only have access to their uh user ID in your database and you know you may not want to make an a a database call to fetch their email or something and it's just easier to use their database ID so in this case it's helpful to to uh capture events with either their email or their uh database ID whichever one is easier for you uh depending on your context okay let's go ahead and Implement post hog Alias before we do I want to show you the code for uh our API route and this is our API that's generating our random number as you can see we pass in the user ID uh as a argument here um and we then use this user ID to capture an event to post hog um the important thing to call out here is that we don't have these as email uh and so this this would be a use case for when you would want to use Adas um because we still want to associate uh this event with the same uh user we identified on the client um and so right now when we call this API rout um and it's capturing these number generator events if we go to our post hog dashboard we see that these um number generator called events are associated with an ID 123 uh and this is just the placeholder ID I've used for the sample app and so this is currently not linked to the max at postal.com um so let's go ahead and implement post hog Alias we're going to do it here on the signin button um You can call Post hog Alias either on the but on the back end or the front end it doesn't matter where you call it so let's call Post hog.

Alias and you need to provide uh two two arguments to this to this function call the first argument is the Alias um alas you want to use so in this case you want to use the user ID and the second argument is the original users original ID um the way you can think about is the original ID is the one the one you use with post hog identify um and so this in our case we used email so the email is the original ID um let's go ahead and we save our changes uh and now let's go to our app uh we sign in so that the edes function will be called and we can start generating a random number uh to call to capture our events in the back end now when we go to our activity Tab and we and we refresh the events we can see here that Alias has been called and even though um in the number generate number generator called event we see that the ID is 1 two three if we click in on this um ID we can see here actually that this is a user with multiple IDs and if we click on this button here we can see all the different IDs for the user so here is the max said post talk email distinct ID which we used for the identify call here is their uh user ID which we passed in one3 and the other three are um are post hog Anonymous IDs that have been created for it and if we click on this users events we can now see uh both the events from the back end uh and the front end events yeah I hope you enjoyed this video uh if you have any questions do comment on the uh doc the docs page for identify uh and we'll be sure to answer you
