# @hogsend/video

## 0.63.0

### Minor Changes

- a61adc0: Keep the engine version line uniform: bump every engine-line package (and the
  `create-hogsend` scaffolder) alongside the change(s) above, so all `@hogsend/*`
  publish on one version and the scaffold's `^{{ENGINE_VERSION}}` caret pins stay
  aligned. Generated by `pnpm release-doctor --fix-changeset`.

## 0.62.0

### Minor Changes

- 766e7f8: `hogsend login` now opens your browser at the approval page with the code
  prefilled, so approving is one click. The code and a bare URL are still always
  printed — a headless machine, an SSH session, or `--no-browser` completes the
  same flow by hand, and a failed browser open never fails the login. The cloud
  approve page renders the code as an eight-box segmented input: prefilled from
  the link, paste-aware (dashed `XXXX-XXXX` or bare), fully keyboard-navigable,
  and screen-reader labelled per character.

### Patch Changes

- a52f9b1: Resolve the client IP for Better Auth's rate limiter behind Railway's edge. Better Auth ≥1.6.25 refuses a multi-valued `x-forwarded-for` when no `trustedProxies` are configured, and Railway always sends two values — so every caller collapsed onto one shared `/sign-in/email` bucket, and ten failed logins by anyone locked the endpoint for everyone. The engine now sets `advanced.ipAddress.ipAddressHeaders` to `["x-real-ip", "x-forwarded-for"]`; `x-real-ip` is single-valued and overwritten by Railway's edge, so it cannot be forged to dodge the limiter.

## 0.61.0

### Minor Changes

- b8429d6: Keep the engine version line uniform: bump every engine-line package (and the
  `create-hogsend` scaffolder) alongside the change(s) above, so all `@hogsend/*`
  publish on one version and the scaffold's `^{{ENGINE_VERSION}}` caret pins stay
  aligned. Generated by `pnpm release-doctor --fix-changeset`.

## 0.60.0

### Minor Changes

- 76da7e4: Keep the engine version line uniform: bump every engine-line package (and the
  `create-hogsend` scaffolder) alongside the change(s) above, so all `@hogsend/*`
  publish on one version and the scaffold's `^{{ENGINE_VERSION}}` caret pins stay
  aligned. Generated by `pnpm release-doctor --fix-changeset`.

## 0.59.0

### Minor Changes

- 84c0fe9: Keep the engine version line uniform: bump every engine-line package (and the
  `create-hogsend` scaffolder) alongside the change(s) above, so all `@hogsend/*`
  publish on one version and the scaffold's `^{{ENGINE_VERSION}}` caret pins stay
  aligned. Generated by `pnpm release-doctor --fix-changeset`.

## 0.58.0

### Minor Changes

- aa24d2a: Keep the engine version line uniform: bump every engine-line package (and the
  `create-hogsend` scaffolder) alongside the change(s) above, so all `@hogsend/*`
  publish on one version and the scaffold's `^{{ENGINE_VERSION}}` caret pins stay
  aligned. Generated by `pnpm release-doctor --fix-changeset`.

## 0.57.0

### Minor Changes

- baa18a2: Keep the engine version line uniform: bump every engine-line package (and the
  `create-hogsend` scaffolder) alongside the change(s) above, so all `@hogsend/*`
  publish on one version and the scaffold's `^{{ENGINE_VERSION}}` caret pins stay
  aligned. Generated by `pnpm release-doctor --fix-changeset`.

## 0.56.0

### Minor Changes

- 0a453d3: Keep the engine version line uniform: bump every engine-line package (and the
  `create-hogsend` scaffolder) alongside the change(s) above, so all `@hogsend/*`
  publish on one version and the scaffold's `^{{ENGINE_VERSION}}` caret pins stay
  aligned. Generated by `pnpm release-doctor --fix-changeset`.

## 0.55.0

### Minor Changes

- 8e57f4d: Keep the engine version line uniform: bump every engine-line package (and the
  `create-hogsend` scaffolder) alongside the change(s) above, so all `@hogsend/*`
  publish on one version and the scaffold's `^{{ENGINE_VERSION}}` caret pins stay
  aligned. Generated by `pnpm release-doctor --fix-changeset`.

## 0.54.0

### Minor Changes

- b62877a: Keep the engine version line uniform: bump every engine-line package (and the
  `create-hogsend` scaffolder) alongside the change(s) above, so all `@hogsend/*`
  publish on one version and the scaffold's `^{{ENGINE_VERSION}}` caret pins stay
  aligned. Generated by `pnpm release-doctor --fix-changeset`.

## 0.53.0

### Minor Changes

- 53757e3: Keep the engine version line uniform: bump every engine-line package (and the
  `create-hogsend` scaffolder) alongside the change(s) above, so all `@hogsend/*`
  publish on one version and the scaffold's `^{{ENGINE_VERSION}}` caret pins stay
  aligned. Generated by `pnpm release-doctor --fix-changeset`.

## 0.52.1

### Patch Changes

- 3b63180: Keep the engine version line uniform: bump every engine-line package (and the
  `create-hogsend` scaffolder) alongside the change(s) above, so all `@hogsend/*`
  publish on one version and the scaffold's `^{{ENGINE_VERSION}}` caret pins stay
  aligned. Generated by `pnpm release-doctor --fix-changeset`.

## 0.52.0

### Minor Changes

- b6b998a: Keep the engine version line uniform: bump every engine-line package (and the
  `create-hogsend` scaffolder) alongside the change(s) above, so all `@hogsend/*`
  publish on one version and the scaffold's `^{{ENGINE_VERSION}}` caret pins stay
  aligned. Generated by `pnpm release-doctor --fix-changeset`.

## 0.51.0

### Minor Changes

- 6242b49: Keep the engine version line uniform: bump every engine-line package (and the
  `create-hogsend` scaffolder) alongside the change(s) above, so all `@hogsend/*`
  publish on one version and the scaffold's `^{{ENGINE_VERSION}}` caret pins stay
  aligned. Generated by `pnpm release-doctor --fix-changeset`.

## 0.50.0

### Minor Changes

- 682c4f0: New package: analytics-first video player — normalized `video.*` watch-depth events across YouTube, Vimeo, and native HTML5, with a pluggable vendor-neutral emitter (Hogsend, PostHog, GA4, Segment, or custom), full player-state access (`getState`, `on("*")`, React hooks), and an open `ProviderAdapter` contract for custom sources.
